# 基礎 URI（二選一：直接填完整 URI，或分別填 BASE_URL 與 PATH）
# Basic URI (choose one: full URI, or BASE_URL + PATH)
# HMAC_URI=https://apidev-extgw.clp.com.hk/transform

# 或分開配置 / Or configure separately
HMAC_BASE_URL=https://apidev-extgw.clp.com.hk
HMAC_PATH=/transform

# HTTP 方法 / HTTP method
HMAC_METHOD=POST

# 應用認證資訊 / Application credentials
HMAC_APP_KEY=your-app-key
HMAC_APP_SECRET=your-app-secret

# 可選：覆蓋請求標頭 / Optional: override request headers
# HMAC_ACCEPT=application/json; charset=utf-8
# HMAC_CONTENT_TYPE=application/x-www-form-urlencoded; charset=utf-8

# 查詢參數（urlencoded：key=value&key2=value2；留空則無）
# Query params (urlencoded: key=value&key2=value2; empty for none)
HMAC_QUERY=

# 表單參數（application/x-www-form-urlencoded：key=value&key2=value2）
# Form params (application/x-www-form-urlencoded: key=value&key2=value2)
HMAC_BODY=username=xiaoming&password=123456789
